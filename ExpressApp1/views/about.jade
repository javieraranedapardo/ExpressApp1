extends layout

block content
  h2 #{title}
  h3 #{message}
  p Use this area to provide additional information.

  table.table.table-bordered
    thead
      tr
        th Nombre
        th Id
    tbody
    each fila in filas
      tr
       td= fila.Nombre
       td= fila.Id
 p hola
